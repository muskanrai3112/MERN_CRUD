const Mode = () => {
  const toggletheme = () => {
    alert("gdgfjsdg");
  };
  return (
    <div>
      <button onClick={() => toggletheme()}>mode</button>`      `
    </div>
  );
};

export default Mode;
